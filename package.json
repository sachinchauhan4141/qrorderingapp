{
  "name": "full-stack-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && cd server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
